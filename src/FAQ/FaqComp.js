import React from "react";
import { useState } from "react";

const FaqComm = (faq, index) => {
    const [isshow, setIsshow]=useState(false);
    useEffect(()=>{
        if(index==0)
            setIsshow((isshow)=>)
    })
    return(
        <div>
            
        </div>
    )
};
export default FaqComm;
